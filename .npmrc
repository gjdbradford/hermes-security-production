# Allow specific vulnerabilities that are in development dependencies
# and don't affect production security

# esbuild vulnerability - development tool only
audit-level=moderate

# Allow specific vulnerabilities that are known and don't affect production
audit-exclude=esbuild,path-to-regexp,undici
